# Across-AI 開發規則

> 後端規範請參考 `backend/README.md`

## AI-First 開發原則

### 命名規範
- 函數名稱必須完整描述其行為（如 `fetchUserProfileByIdAndValidatePermissions` 而非 `getUser`）
- 變數名稱必須清楚表達用途和類型（如 `isUserAuthenticatedAndHasAdminRole` 而非 `isAuth`）
- 禁止使用縮寫或模糊命名（如 `usr`、`tmp`、`data`、`info`、`handler`）
- 參數名稱必須自我解釋（JSDoc 用於業務 context，不是解釋參數用途）

### 檔案組織
**按功能模組拆分，同一功能內不過度拆分**

拆分原則：
- 不同功能模組 = 不同檔案（如 user/、workflow/、report/）
- 同一功能的 type、schema、api、helpers 放在同一檔案
- 禁止為了「人類方便瀏覽」把一個功能拆成多個小檔案

原因（AI context 優化）：
- AI 讀取特定功能時，只載入該功能的檔案
- 該檔案的註解提供該功能完整的 context
- 避免 AI 為了理解一個功能要讀取多個檔案

```
services/
├── user/index.ts       # 用戶功能：type + api + helpers 都在這
├── workflow/index.ts   # 工作流功能：獨立模組
└── report/index.ts     # 報告功能：獨立模組
```

### 代碼結構
- 相關的 type、schema、fetcher、常數放在同一檔案
- **抽象越少越好**：直接實作優先，禁止創建只用一次的 helper/wrapper/adapter
- **結構越扁平越好**：減少目錄嵌套、減少函數嵌套、減少 callback 層級

### 註解策略（為 AI 提供 Context）
註解的目的是提供 AI 執行任務所需的上下文，提高產出準確性。

**必須註解的內容：**
- 業務邏輯背景（為什麼這樣設計、業務規則是什麼）
- 邊界條件和特殊情況（什麼情況下會失敗、有什麼限制）
- 依賴關係（這段代碼依賴哪些外部系統、API、或其他模組）
- 資料流向（資料從哪來、經過什麼轉換、到哪去）
- 歷史決策（為什麼不用其他方案、之前踩過什麼坑）

**註解範例：**
```typescript
/**
 * 獲取用戶的 WAF 分析報告
 * 
 * 業務背景：此 API 整合 Cloudflare 和 F5 兩個 WAF 來源的數據，
 * 用戶可能只有其中一個服務，所以兩個來源都要處理 404 的情況。
 * 
 * 資料流：前端 → backendClient → Node.js 後端 → ELK MCP → Elasticsearch
 * 
 * 邊界條件：
 * - 時間範圍超過 30 天會導致 ES 查詢超時，後端已限制最大 30 天
 * - 用戶沒有該服務權限時返回空陣列，不是 403
 * 
 * 依賴：需要 ELK MCP Server 運行中，連線設定在 backend/.env
 */
export async function fetchWAFAnalysisReport(
  userId: string,
  timeRangeInDays: number
): Promise<WAFAnalysisReport> {
  // ...
}
```

**禁止的註解：**
- 重複代碼本身（如 `// 設定 name 為 value` 配 `name = value`）
- 沒有 context 價值的說明（如 `// 這是一個函數`）

## 強制規則

### API 調用
- 認證相關 API：使用 `authClient`（來自 `@/lib/api-clients`）
- 業務 API：使用 `backendClient`（來自 `@/lib/api-clients`）
- 禁止：直接使用 `fetch`、創建新 Axios 實例、硬編碼 URL

### 新增 API 功能
位置：`services/[feature]/index.ts`（單一檔案包含 schema + api）

```typescript
// services/user/index.ts - Schema、類型、API 都在同一檔案
import { z } from 'zod';
import { authClient } from '@/lib/api-clients';

// ===== Schema 和類型 =====
export const UserResponseSchema = z.object({
  id: z.string(),
  name: z.string(),
});
export type UserResponse = z.infer<typeof UserResponseSchema>;

// ===== API 函數 =====
/**
 * 業務背景：獲取單一用戶資料，用於個人資料頁和權限檢查
 * 依賴：authClient（認證服務）
 * 邊界條件：用戶不存在時返回 404
 */
export async function fetchUserById(userId: string): Promise<UserResponse> {
  const response = await authClient.get(`/users/${userId}`);
  return UserResponseSchema.parse(response.data);
}

// ===== 組件中使用（不創建額外 hooks 層）=====
// import { useQuery } from '@tanstack/react-query';
// import { fetchUserById } from '@/services/user';
//
// const { data, isLoading } = useQuery({
//   queryKey: ['user', userId],
//   queryFn: () => fetchUserById(userId),
// });
```

禁止：在 `app/routes/` 新增功能（舊架構，僅維護）

### TypeScript
- 禁止：`any` 類型
- 禁止：`as` 類型斷言（使用 Zod 驗證或類型守衛替代）
- 使用：`unknown` + 類型守衛、Zod Schema 驗證
- 使用：嚴格模式（`strict: true`）
- 優先：`type` 而非 `interface`

### 環境變數
- 禁止：使用預設值 fallback（如 `process.env.X || 'default'`）
- 必須：未設定時拋出明確錯誤
- 客戶端變數：必須有 `NEXT_PUBLIC_` 前綴
- 詳細配置：參考 `frontend/.env.example`

### 錯誤處理
- `authClient`：自動處理 401/403（觸發登出）
- `backendClient`：不處理認證錯誤
- 所有錯誤必須有用戶可理解的提示

## 檔案位置

| 類型 | 位置 |
|-----|------|
| API 客戶端定義 | `lib/api-clients.ts` |
| 新 API 實作 | `services/[feature]/index.ts`（單一檔案） |
| 可重用 UI 組件 | `components/` |
| 頁面組件 | `app/[page]/` |
| 舊 API 封裝（不新增） | `app/routes/` |

## 代碼風格

- 語言：繁體中文（註解、回應、錯誤訊息）
- 格式：遵循 Biome 配置
- 函數：必須有 JSDoc（業務背景、邊界條件、依賴關係，不是解釋參數）
- 命名：組件 PascalCase、函數/變數 camelCase、常數 UPPER_SNAKE_CASE
- 註解原則：為 AI 提供 context，而非解釋代碼字面意思

## 錯誤示範與正確做法

```typescript
// ❌ 錯誤
const res = await fetch('http://localhost:3001/api/users');
const user = response.data as User;
function process(data: any) {}
const url = process.env.API_URL || 'http://localhost';

// ✅ 正確
const response = await authClient.get('/users');
const user = UserSchema.parse(response.data);
function processUserData(userData: unknown) {
  const validatedUser = UserSchema.parse(userData);
}
const url = getRequiredEnvVariable('API_URL'); // 內部拋出錯誤
```
